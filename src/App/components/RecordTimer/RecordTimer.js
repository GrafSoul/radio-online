import React from 'react';

import classes from './RecordTimer.module.scss';

const RecordTimer = () => {
    return <span className={classes.recordTime}>00:00:00:00</span>;
};

export default RecordTimer;
